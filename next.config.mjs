/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost','cms.garden4all.org','garden4all.org','cdn.sanity.io',"cdn.ucc.edu.gh"]
    }
};

export default nextConfig;
